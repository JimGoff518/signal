# SIGNAL — Python dependencies (Phase 1 MVP).
# Floors only; pip resolves up. We work on Python 3.14, so several libraries
# need their newest releases (pandas 2.3+, etc.).

# Web dashboard — FastAPI is the production server.
# Streamlit kept around until the FastAPI build is verified at parity.
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.20
itsdangerous>=2.2
streamlit>=1.40

# Database
psycopg[binary]>=3.2
sqlalchemy>=2.0

# HTTP client (NHTSA API)
httpx>=0.28

# AI viability memos
anthropic>=0.39
langsmith>=0.2

# Email alerts
resend>=2.4

# Config / env
python-dotenv>=1.0
pydantic>=2.10
pydantic-settings>=2.7

# Data wrangling
pandas>=2.3

# Charts
altair>=5.5

# CLI / scheduling helpers
click>=8.1

# Logging
structlog>=24.4

# Testing
pytest>=8.3
pytest-cov>=6.0
